{
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./assets/css/styles.css -o ./assets/dist-styles.css --minify",
    "watch:css": "npx @tailwindcss/cli -i ./assets/css/styles.css -o ./assets/dist-styles.css --watch",
    "build:site": "BUNDLE_PATH=vendor/bundle BUNDLE_FORCE_RUBY_PLATFORM=true bundle exec jekyll build -d _site",
    "smoke:site": "./scripts/smoke-check.sh _site",
    "serve": "BUNDLE_PATH=vendor/bundle BUNDLE_FORCE_RUBY_PLATFORM=true bundle exec jekyll serve"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.10",
    "tailwindcss": "^4.1.10"
  },
  "devDependencies": {
    "daisyui": "^5.0.43"
  },
  "packageManager": "bun@1.3.10"
}
